ing=\"lazy\" decoding=\"async\" />` +
    `</picture>` +
    `</div>` +""",
    """    `<img class=\"blog-figure-img\" src=\"${figureFile(hash, 'png')}\" ` +
    `srcset=\"${figureFile(hash, 'svg')} 1x\" alt=\"${alt}\"${dimensions} ` +
    `loading=\"lazy\" decoding=\"async\" />` +""")

sub(C, """ * Build time rendered figures, published as files and referenced with
 * <picture>. Every browser takes the SVG source; the PNG exists for readers
 * that cannot carry SVG.""",
    """ * Build time rendered figures, published as files. Every browser takes the SVG
 * from srcset; the PNG in src exists for readers and importers that cannot
 * carry SVG.""")

sub(C, """.blog-prose .blog-figure-svg {
  overflow-x: auto;
}

.blog-prose .blog-figure img,""", """.blog-prose .blog-figure img,""")

sub(C, """

.blog-prose .blog-figure picture {
  display: block;
}
""", """
""")

print('patched')
